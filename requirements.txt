boto3>=1.20.0
Pillow>=9.0.0
opencv-python>=4.5.0
# Camera access uses cv2.VideoCapture (V4L2 legacy camera driver).
# On Raspberry Pi Zero, enable the legacy camera interface first:
#   sudo raspi-config -> Interface Options -> Legacy Camera -> Enable
# Then the Pi camera will appear as /dev/video0.
